import 'package:flutter/material.dart';

class ExerciseTwoPage extends StatelessWidget {
  const ExerciseTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exercise 2: Weekly Salary Calculation'),
      ),
      body: Center(
        child: Text('Exercise 2 content goes here'),
      ),
    );
  }
}
