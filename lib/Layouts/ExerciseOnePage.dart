import 'package:flutter/material.dart';

class ExerciseOnePage extends StatelessWidget {
  const ExerciseOnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exercise 1: Customer Balance Calculation'),
      ),
      body: Center(
        child: Text('Exercise 1 content goes here'),
      ),
    );
  }
}
